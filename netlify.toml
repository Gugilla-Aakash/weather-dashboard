[build]
  publish = "dist" # or "." / "public" / whatever folder your site is served from
  command = "npm run build" # if you don't have a build step set to "" or remove

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
